import TagArrayMap from "./maps/TagArrayMap";

export default class Tagable {
    constructor(tagArrayMap = new TagArrayMap()) {
        this.tagArrayMap = tagArrayMap;
    }

    static getTagArrayMap
}